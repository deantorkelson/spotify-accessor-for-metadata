export enum ENVIRONMENTS {
  DEV="DEV",
  PROD="PROD",
}

const HOME = '/spotify-accessor-for-metadata';

export const ROUTES = {
  HOME: `${HOME}`,
  SEARCH: `${HOME}/search`,
  COMPARE: `${HOME}/compare`,
}